Django==5.2.7
python-dotenv==1.1.1
djangorestframework==3.16.1
django-crispy-forms==2.4
crispy-bootstrap5==2025.6
Pillow==11.3.0

# Dependências do verificador (migrado do Flask)
pandas>=2.0.0
openpyxl>=3.0.0
requests>=2.28.0

# Dependências adicionais do Verificador-De-Viabilidade-main
lxml>=6.0.0      # Para parsing XML/KML
xlrd>=2.0.0      # Para ler arquivos XLS antigos

# Cache e performance (opcional)
django-redis>=5.2.0  # Para cache Redis (opcional)
dj-database-url==2.3.0
psycopg[binary]==3.2.3

# Servidor WSGI para produção
gunicorn>=21.2.0  # Linux/Mac
waitress>=2.1.2  # Windows (alternativa ao gunicorn)

# Servir arquivos estáticos em produção
whitenoise>=6.5.0

# Monitoramento (opcional)
# sentry-sdk>=1.32.0  # Descomente se usar Sentry